# Build-time API URL for Cloudflare Pages / static hosts without edge proxy.
# Leave empty for local dev (Vite proxy) or Vercel with CORTEX_API_ORIGIN middleware.
VITE_API_URL=

# Optional default key shown in Connection panel (overridable in UI).
VITE_CORTEX_API_KEY=
